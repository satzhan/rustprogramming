[package]
name = "dispatcher_fifo"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
rand_chacha = "0.3"   # seeded, reproducible RNG
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[profile.release]
opt-level = 3
